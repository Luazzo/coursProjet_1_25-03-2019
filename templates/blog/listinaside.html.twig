{% if blogs %}
	<ul class="recent-posts">
		{% for blog in blogs %}
			<li><a href="{{path('app_blog_show', {id : blog.id}) }}">{{blog.name.getContent(app.request.locale)}}</a></li>
		{% endfor %}
	</ul>
{% endif %}
